export type tableStyle = {
  id: string;
  position: string;
  isRound: boolean;
  capability: string;
};

export type cellType = {
  startCell: number;
  endCell: number;
};

export type showDetailsState = {
  id: string;
  isShow: boolean;
};
